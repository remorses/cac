<div align='center'>
    <br/>
    <br/>
    <h3>unframer</h3>
    <p>useful framer plugins </p>
    <br/>
    <br/>
</div>

## Todos

- mcp
- when
-


- need to add ways to use these features via xml:
    - apply color styles instead of simple color strings
        - text color
        - background color
        - border color
    - apply an image to a image node or image background via image url. will require work from the framer xml applier client side to download the image, upload to framer and set it
    - apply text styles to text nodes instead of just inline text styles
